[project]
name = "sexparty-backend"
version = "0.1.0"
description = "FastAPI backend for the SexParty watch-party platform."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
